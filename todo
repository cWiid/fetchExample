- make header checkbox select everything
- start view at first task
- fixed header clipping issue
- make labels line up with general input fields better
- make column padding so that mon doesn't touch tue and etc.
- delete button goes floats left
- select menus have dropdown arrows
- modal tables stay consistent
- Save changes is disabled until change is actually made


- only send orgData["routines"] back and forth between py and js

!!
REFACTOR TO KEY -> ID
add data-key and data-value for all inputs